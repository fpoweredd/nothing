  // Crea un array llamado heroes que contenga 4 elementos de tipo String
const heroes = ["Superhero", "Megahero", "Nothero", "JShero"]
  // Muestra por consola todo el contenido del array  
console.log(heroes)
  // Muestra por consola la longitud de dicho array
console.log(heroes.length)
  // Ahora, muestra por consola el primer elemento del array heroes
console.log(heroes[0])
  // Cambia el segundo elemento del array por otro héroe nuevo y muéstralo por consola
heroes[1] = "Fakehero"
  // Agrega un valor de tipo de Number con el método push y muestra el array entero de nuevo
heroes.push(1234567890)
  // Vuelve a mostrar la longitud del array
console.log(heroes.length)
console.log(heroes)
